# Database — Application (NO BYPASSRLS)
DATABASE_URL=postgresql://rasid_app:rasid_app_pass@localhost:5432/rasid_platform?search_path=kernel

# Database — Admin (BYPASSRLS — migrations and seeds ONLY)
DATABASE_ADMIN_URL=postgresql://rasid_admin:rasid_admin_pass@localhost:5432/rasid_platform?search_path=kernel

# JWT
JWT_SECRET=change-me-in-production-min-32-chars!!
JWT_ACCESS_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
