/**
 * Seed script — will be implemented in K1 (STEP 2).
 * Phase 0 STEP 1 only sets up the migration infrastructure.
 */
console.log('Seed script placeholder — will be implemented in STEP 2 (K1 IAM).');
process.exit(0);
